/* Media query for mobile devices */
@media (max-width: 767px) {
    /* Center the logo */
    .logo {
        display: block;
        margin: 0 auto; /* Center horizontally */
    }

    .nav {
        position: absolute;
        top: 100px; /* Increased value to move the navigation menu down */
        left: 40px;
        list-style: none;
        padding: 10px;
    }

    /* Remove border from top of navigation links */
    .nav a::before {
        content: none;
    }

    .nav a:hover,
    .nav li.active a {
        color: #0065FC; /* Change color on hover and for active links */
        border-bottom: 2px solid #0065FC; /* Add blue border line underneath on hover and for active links */
    }
}

.heading h1 {
    font-size: 20px; /* Adjust font size for mobile */
    margin-top: 30px;
    margin-bottom: 8px; /* Adjust margin for mobile */
    margin-left: 0px;
}

.heading h2 {
    font-size: 18px; /* Adjust font size for mobile */
    margin-left: 0px;
    margin-top: 5px;
}

 /* Adjust margin for search container on mobile */
    .search-container {
        margin-top: 20px;
        margin-left: -50px;
    }

    /* Adjust padding for search bar on mobile */
    .search-bar {
        padding: 8px;
    }

    /* Adjust font size for search input on mobile */
    .search-input {
        font-size: 14px;
    }

    .filters {
        width: 100%;
        padding-left: 2px;
        padding-right: 5%;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
    }
    
    .filter-label {
        font-size: 16px;
        flex-basis: 100%;
        margin-top: -5px;
        margin-left: -20px;
    }
    
    .filter {
        margin-left: 5px;
        margin-bottom: 5%;
        display: flex;
        align-items: center;
        max-height: 75px;
        border-radius: 50px;
    }
    
    .filter-text {
        font-size: 4.5vw;
        padding-left: 3vw;
        padding-right: 3vw;
    }
    
    .filter-icon {
        font-size: 4.5vw;
        padding: 18px;
        margin-right: 0px;
        margin-left: -18px;
    }
    
    .accommodations-header {
        font-size: 16px; /* Adjust font size for mobile */
        margin-left: -15px; /* Adjust margin for mobile */
    }
    
    .accommodations-icon {
        font-size: 20px; /* Adjust icon size for mobile */
    }
